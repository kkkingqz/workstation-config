# Allow the active desktop user / input group to create uinput devices.
# Required by xremap when it runs as an unprivileged systemd --user service.
KERNEL=="uinput", GROUP="input", TAG+="uaccess", MODE:="0660", OPTIONS+="static_node=uinput"
