# The Touch Bar stays in native keyboard mode (see /etc/udev/rules.d/90-touchbar-native.rules
# and docs/touchbar.md in workstation-config), so its display driver is not needed; keep it
# from binding if configuration 2 ever comes up.
blacklist appletbdrm
