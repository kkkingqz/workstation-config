# Patch files keep blank diff context lines (a single space) verbatim.
kernel/**/*.patch -whitespace
# Captured byte-for-byte from the running system; keep their trailing blank lines.
system/boot/refind_linux.conf -whitespace
system/default/grub.d/99-recovery-menu.cfg -whitespace
system/usr/local/sbin/system-backup-snapshot -whitespace
