# apt packages of mbp16 (MacBookPro16,1, T2) on top of hosts/apt.txt.
# The T2 repository must already be configured (rebuild.md, section 4).

# T2 kernel (held: patched t2bce modules exist per kernel, docs/suspend.md)
linux-t2
apple-t2-audio-config
apple-firmware-script
